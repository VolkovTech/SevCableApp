<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andreyvolkov.sevcableapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Model.VkApplication"
        android:allowBackup="true"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Activity.VkAuthActivity" />
        <activity android:name=".View.Activity.WelcomeActivity" />
        <activity android:name=".View.Activity.CustomerActivity" />
        <activity android:name=".View.Activity.ResidentActivity"></activity>
    </application>

</manifest>